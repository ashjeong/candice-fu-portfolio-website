import { createTheme } from "@mui/material/styles";

export const websiteTheme = createTheme({
  palette: {
    primary: {
      main: "#FF5115",
    },
    secondary: {
      main: "#646CFF",
    },
  },
});
